О-нотация — асимптотическое время работы вместо точного (часто его ещё называют просто _асимптотикой_).
В контексте анализа алгоритмов, фраза «алгоритм работает за O(f(n)) операций» означает, что начиная с какого-то nn он делает не более чем за c⋅f(n)c⋅f(n) операций.
# Мастер-теорема
