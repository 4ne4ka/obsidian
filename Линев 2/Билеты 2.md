## 1. Операционная система (определение + 2 точки зрения на ОС)
*(Лекция 2, раздел "Введение в операционные системы")*

**Определение:** ОС — это комплекс программ, управляющий аппаратными ресурсами компьютера и предоставляющий прикладным программам удобное окружение для выполнения.

**Две точки зрения на ОС:**

1.  **ОС как менеджер ресурсов:**
    - Управляет распределением всех ресурсов вычислительной системы (CPU, память, диски, сеть, устройства ввода-вывода).
    - Отслеживает состояние (свободен/занят), планирует использование, разрешает конфликты.

2.  **ОС как виртуальная машина:**
    - Предоставляет программам абстракции, скрывая сложность реального «железа».
    - Примеры абстракций: диск → файловая система; принтер → очередь печати; оперативная память → процесс; CPU → поток.

## 2. Жесткие диски (преимущества перед перфокартами и перфолентами, spooling)
*(Лекция 2, раздел "Эволюция ОС")*

**Преимущества жестких дисков:**
- **Произвольный доступ:** В отличие от магнитной ленты, где для доступа к блоку С нужно перемотать ленту от А через В (секунды), у диска время доступа — доли секунды (перемещение головок + ожидание сектора).
- Возможность хранить структуры данных с произвольным доступом (например, иерархическую файловую систему).

**Spooling (Simultaneous Peripheral Operations On-Line):**
- **Исторически:** Пока одна задача выполняется (CPU), следующая задача параллельно загружается с медленного устройства (перфокарты, ленты) на быстрый диск. Это кэширование задач.
- **Сейчас:** Означает управление очередями. Пример — очередь печати (служба Pooler в Windows): несколько программ отправляют данные не на принтер напрямую, а в очередь, и сервис печати последовательно их отправляет.

## 3. Операционная среда (определение + условия выполнения программ)
*(Лекция 2, раздел "Введение в операционные системы")*

**Определение:** Операционная среда — это совокупность условий, в которых выполняется прикладная программа.

**Условия выполнения (4 характеристики):**
1.  **Система команд:** Множество команд CPU, доступных программе.
2.  **Адресное пространство:** Модель адресации, структура и объем памяти (код, данные, стек).
3.  **Системные вызовы:** Набор служебных функций, которые ОС предоставляет программе.
4.  **API (Application Program Interface):** Интерфейс прикладного программирования — способ взаимодействия программы со средой.

## 4. Многозадачность (определение + преимущество). ОС разделения времени (описание)
*(Лекция 2, раздел "Эволюция ОС")*

**Многозадачность:**
- **Определение:** В оперативной памяти одновременно находятся несколько программ (задач). Когда одна программа ждет ввод-вывод, ОС переключает CPU на другую программу.
- **Преимущество:** Повышается общая пропускная способность системы (CPU не простаивает в ожидании медленных операций ввода-вывода).
- *Ограничение:* Эффективна при разнотипной нагрузке. При конкуренции за один ресурс (например, две вычислительные задачи) может быть даже медленнее из-за накладных расходов на переключение контекста.

**ОС разделения времени (Time-Sharing):**
- **Описание:** Один мощный компьютер обслуживает нескольких пользователей, каждый за своим терминалом работает так, как будто компьютер принадлежит только ему.
- **Реализация:** Используется обычная многозадачность. Планировщик ОС распределяет время CPU между всеми задачами от разных пользователей. Главный критерий — отзывчивость (задержка не более ~10 мс незаметна для человека).
- **Примеры:** CTSS, Multics.

## 5. Классификация ОС по назначению
*(Лекция 3, раздел "Классификация ОС по назначению")*

1.  **Системы пакетной обработки:** Максимальная пропускная способность. Выполняют задачи одну за другой. Минимум взаимодействия с пользователем. (Сейчас — в системах управления кластерами).
2.  **Системы разделения времени:** Удобство и эффективность работы пользователя. Быстрое переключение между задачами, интерактивность.
3.  **Системы реального времени (ОСРВ):** Способность выдерживать заданные временные интервалы между событием и реакцией. Важна не просто быстрота, а гарантированность. Пример: управление станком, подушками безопасности.

## 6. Классификация ОС по режиму обработки задач
*(Лекция 3, раздел "Классификация по режимам обработки задач")*

**По поддержке многозадачности:**
- **Однозадачные:** В каждый момент выполняется одна программа (MS-DOS).
- **Многозадачные:** Могут выполняться несколько программ одновременно (Unix, Windows).

**Многозадачные делятся на:**
1.  **Невытесняющая (кооперативная):** Программа сама добровольно возвращает управление ОС (при завершении, вводе-выводе, вызове `yield`). Если программа зависнет — зависнет вся система (Windows 3.x).
2.  **Вытесняющая:** ОС выделяет квант времени. По истечении кванта принудительно забирает CPU. Надежнее и отзывчивее (Windows NT, Unix).

**По поддержке многопоточности:** ОС может поддерживать несколько потоков внутри одного процесса.

**По поддержке многопроцессорной обработки:** Способность использовать несколько CPU/ядер.

## 7. Классификация ОС по организации пользовательской работы
*(Лекция 3, раздел "Пользователи, сессии и архитектура ОС")*

**По количеству пользователей:**
- **Однопользовательские:** Один физический пользователь.
- **Многопользовательские:** Главное — **разграничение прав доступа и защита данных одного пользователя от другого**, даже если они работают в разное время (ACL, права доступа к файлам).

**По количеству сессий:**
- **Односессионные:** Одна среда работы пользователя.
- **Многосессионные:** Несколько сессий одновременно. Пример: в Linux — 6 локальных текстовых терминалов (TTY) + множество удаленных (SSH). В Windows — RDP-подключения.

## 8. Расширяемость, Переносимость, Совместимость, Безопасность
*(Лекция 3, раздел "Дополнительные критерии и характеристики ОС" + "Безопасность")*

**Расширяемость:** Возможность внесения изменений без нарушения целостности. Способы: наличие исходных кодов, загрузка модулей ядра (драйверы), хорошая декомпозиция кода.

**Переносимость:** Возможность перенести ОС на другую аппаратную платформу. Достигается: языки высокого уровня (C/C++), минимизация ассемблера, локализация низкоуровневого кода (каталог `arch/` в Linux).

**Совместимость:** Способность выполнять программы, написанные для других ОС или версий. Уровни: бинарная (`.exe` без перекомпиляции) и на уровне исходных текстов. Обратная совместимость критически важна для Windows.

**Безопасность (уровень C2):**
*   **Идентификация и вход.** Защищенный ввод логина и пароля.

*   **Права доступа (DAC).** Владелец файла сам решает, кому его открыть. В Windows это списки ACL: «Васе можно читать, Пете — нет». Запрет (Deny) всегда главнее разрешения (Allow).

*   **Аудит (слежка).** Можно включить запись в журнал того, кто и когда трогал файл

*   **Защита памяти.** Когда программа освобождает память, система должна стереть там всё. Иначе хакер может выключить компьютер, вытащить диск и прочитать файл подкачки (pagefile.sys), где могли остаться ваши пароли. По умолчанию в Windows очистка этого файла выключена, чтобы компьютер быстро выключался.

*   **Права (Permissions) vs Привилегии (Privileges).**
    *   *Права* — это «можно ли Пете открыть этот конкретный файл».
    *   *Привилегия* — это «можно ли Пете менять системное время или повышать приоритет своей программы». Это особые системные полномочия.

*   **Функциональная избыточность.** В любой ОС одну и ту же задачу (например, передать данные из программы А в программу Б) можно решить кучей способов: через файл, канал (pipe), сокет, разделяемую память (самый быстрый, но сложный), буфер обмена, очередь сообщений и даже почтовые ящики (mailslots).
## 9. Монолитная архитектура ОС
*(Лекция 3, раздел "Особенности архитектуры ОС")*

**Определение:** ОС — это одна большая программа. Все компоненты работают на одном уровне привилегий (в режиме ядра) и имеют доступ ко всем данным.

**Признаки:**
- Единое пространство (все в режиме ядра).
- Общие данные (глобальные переменные доступны отовсюду).
- Непосредственные вызовы функций (как в обычной программе).

**Плюсы:** Простота на начальном этапе, высокая производительность (нет накладных расходов на IPC).

**Минусы:** Сложность понимания и разработки (большой код), ошибка в любом компоненте (даже драйвере) может разрушить всю систему.

**Примеры:** Linux, Windows, FreeBSD.

## 10. Микроядерная архитектура ОС
*(Лекция 3, раздел "Типы архитектур ядра")*

**Определение:** Минимизировать объем кода, работающего в режиме ядра. В ядре остается только самый необходимый минимум (планировщик, базовый IPC, обработка прерываний). Всё остальное (ФС, драйверы, сеть) выносится в пользовательский режим как «защищенные подсистемы» (сервисы).

**Плюсы:**
- Надежность: упал сервис ФС — можно перезапустить, ядро живо.
- Простота разработки: сервисы независимы.
- Компонуемость: можно собрать нужную конфигурацию (маркетинг).

**Минусы:** Низкая производительность из-за множества дорогих переключений (приложение → микроядро → сервис ФС → микроядро → приложение).

**Примеры:** QNX, Symbian, Mach (влияние на macOS/iOS).

## 11. Многоуровневая (слоеная) архитектура ОС
*(Лекция 3, раздел "Особенности архитектуры ОС")*

**Определение:** ОС организуется иерархически. Каждый уровень взаимодействует только со своими непосредственными соседями (выше- и нижележащим).

**Плюсы:** Декомпозиция сложной задачи на простые подзадачи. Упрощение разработки и отладки. Широко применяется в прикладном ПО и внутри подсистем ОС.

**Минусы:** Накладные расходы на транзитные вызовы через несколько уровней.

**Пример:** Сетевой стек (сервисы → протоколы → драйверы → сетевая карта). Потери незаметны, так как скорость CPU >> скорости сети.

**«Проклятие дырочки»:** Если пробить уровень и вызвать нижний напрямую — теряются все преимущества многоуровневости и возвращаются недостатки монолита.

## 12. Архитектура ОС, основанная на использовании виртуальных машин. Паравиртуализация
*(Лекция 3, раздел "Виртуализация")*

**Программная (аппаратная) виртуализация:**
- Гипервизор (VirtualBox, VMware) эмулирует виртуальное аппаратное обеспечение.
- Гостевая ОС работает поверх этой эмуляции.
- **Недостаток:** Огромные накладные расходы (замедление до 10 раз).

**Паравиртуализация:**
- Гостевая и хостовая ОС должны быть **одними и теми же** (специально модифицированными).
- Вместо эмуляции железа — специальный API для прямого обращения к гипервизору.
- Для критичных операций (диск, сеть) — прямое отображение запросов, минуя эмуляцию.
- **Преимущество:** Потери всего 10-15%.